--- version beta3:

* implement system config tables:
   - saison
   - groupes (& instructor)
   - escomptes
   - frais -> table for keeping old frais around
   - suggested dates for cheques

* export to CSV or XLS, especially per class
* lists print out wrong grade when multi grades are present?

* nightly dumps of database to remote locations

* edit view of class list

2- extraction des listes de judoka selon leur cours, ou grade, ou age etc
3- automatisation du formulaire FT-303 lors d'inscription de competition (nom, age, grade, lieu de competition etc - il ne manquerait que la signature du directeur) (J'ai fait cela l'an passé mais ca ne sortait pas directement d'une base de donnée mais il fallait entrer les noms etc - au moins cela a aider Rejean a ne pas a avoir à remplir toutes ces données)
4- information par envoi de courriel automatisé.

--- later:

* reduce number of requests

* autosuggest for payment groups

* implement merges/conflicts

* use database for user login authentication, not stuff in textfiles!

* implement table for tournament results

